# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Python/uv/venv
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.python-version
.mypy_cache/
.pytest_cache/
.coverage
htmlcov/
.ruff_cache/
.tox/
.hypothesis/
.env
.envrc

# Ansible/automation
*.retry
*.vault
ansible/vault_config.yml
*.secret
*.enc
*.age
.ansible/
.ansible-lint
.ansible-lint-ignore
.ansible-lint-cache/
.cache/
.vault-token
*vault-token*

# Temporary/sensitive files
/tmp/vault_update.py
*~
.#*
.swp
.swo
.bak
*.log
nohup.out

# Editor/IDE
.vscode/
.idea/
.history/
.nvim/
.spyderproject
.spyproject
.ropeproject
.project
.settings/
.sublime*
.code-workspace

# OS artifacts
.Trash-*
Thumbs.db
ehthumbs.db
Desktop.ini
.fuse_hidden*

# Project-specific
host_vars/
# group_vars/   # <-- Commented out to allow group_vars to be tracked by git
ansible/backups/
**/backups/

# Ansible-specific ignores
# Secrets and sensitive files
*.vault_pass.txt
*.key
*.pem
*.crt

# Backup files that may contain sensitive configurations
**/backups/  # Any backup directories throughout the project

# Inventory and host_vars/group_vars that may contain secrets
host_vars/
# group_vars/   # <-- Commented out to allow group_vars to be tracked by git

# SSH and security artifacts
*.pub

# Miscellaneous
*.swp
*.swo
__pycache__/

# Ignore output or result files generated by playbooks/scripts unless you want to track them

# Add any additional ignores for Docker, Terraform, etc. as needed

# MCP-related ignores
/Cline/MCP/*/
!/Cline/MCP/*/README.md
